import initialData from './initial-charge-data.json';

import { hash } from 'bcryptjs';

// const createSuperUser = async (): Promise<void> => { };

(async () => {
  // await createSuperUser();
})();
